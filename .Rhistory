c = load(cars)
cars
2+2
library(help="datasets")
women
b = "brittanderson"
b[0]
typeof(b)
split(b)
strsplit(b)
strsplit(b,"")
for (l in strsplit(b,"")){
   print(l)
}
order(strsplit(b,""))
sort(strsplit(b,""))
?order
order(unlist(strsplit(b,"")))
unlist(strsplit(b,""))[order(unlist(strsplit(b,"")))]
        myName = "brittAnderson"
        myList = unlist(strsplit(b,""))

        for (l in myList){
          print(l)
        }



        for (l in myList[order(myList)]){
          print(l)
        }

        i = 1
        for (n in order(myList)){
          sprintf("The %ith letter of myList is: %s, but is %s in the sorted list.",i,myList[i],myList[n])
          i = i+1  
          }

        for (n in order(myList)){
          print(sprintf("The %ith letter of myList is: %s, but is %s in the sorted list.",i,myList[i],myList[n]))
          i = i+1  
          }
 for (n in order(myList)){
order(myList)
order(myList)[0]
order(myList)[1]
for (n in order(myList)){
   print(n)
}
help(sprintf)
quit()\
quit()
n
ls()
ls()
a = 1
ls()
ls()
names(d)
quit()
n
